# mind root data path
MIND_DATA_PATH: /path/to/data/path

PROJECT_NAME: DMT_IRB

DATASET_NAME: DMT_IRB_DATE

# num partition for delta table
NUM_PARTITION: 240

# sql string to filter dicom metadata. To be removed once correct metadata to identify scan is around..
SQL_STRING: metadata.SeriesDescription LIKE 'PH1%AX%'

# crop image width, height
CROP_WIDTH: 256

CROP_HEIGHT: 256

# original png image width, height
IMAGE_WIDTH: 512

IMAGE_HEIGHT: 512
